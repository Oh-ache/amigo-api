// Code generated by goctl. DO NOT EDIT.
// versions:
//  goctl version: 1.8.2

package model

import (
	"context"
	"database/sql"
	"fmt"
	"strings"
	"time"

	"amigo-api/common/utils"

	"github.com/zeromicro/go-zero/core/stores/builder"
	"github.com/zeromicro/go-zero/core/stores/cache"
	"github.com/zeromicro/go-zero/core/stores/sqlc"
	"github.com/zeromicro/go-zero/core/stores/sqlx"
	"github.com/zeromicro/go-zero/core/stringx"
)

var (
	baseCodeItemFieldNames          = builder.RawFieldNames(&BaseCodeItem{})
	baseCodeItemRows                = strings.Join(baseCodeItemFieldNames, ",")
	baseCodeItemRowsExpectAutoSet   = strings.Join(stringx.Remove(baseCodeItemFieldNames, "`base_code_item_id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), ",")
	baseCodeItemRowsWithPlaceHolder = strings.Join(stringx.Remove(baseCodeItemFieldNames, "`base_code_item_id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), "=?,") + "=?"

	cacheAmigoBaseCodeItemBaseCodeItemIdPrefix = "cache:amigo:baseCodeItem:baseCodeItemId:"
)

type (
	baseCodeItemModel interface {
		Insert(ctx context.Context, data *BaseCodeItem) (sql.Result, error)
		FindOne(ctx context.Context, baseCodeItemId uint64) (*BaseCodeItem, error)
		FindOneBySortKeyKey(ctx context.Context, sortKey, key string) (*BaseCodeItem, error)
		Update(ctx context.Context, data *BaseCodeItem) error
		Delete(ctx context.Context, baseCodeItemId uint64) error
		CheckDuplicate(ctx context.Context, data *BaseCodeItem) (bool, error)
		List(ctx context.Context, search *BaseCodeItemSearch) ([]*BaseCodeItem, int64, error)
	}

	defaultBaseCodeItemModel struct {
		sqlc.CachedConn
		table string
	}

	BaseCodeItem struct {
		BaseCodeItemId uint64 `db:"base_code_item_id"`
		SortKey        string `db:"sort_key"`   // 标准码分类key
		Key            string `db:"key"`        // 标准码key
		ForeignId      uint64 `db:"foreign_id"` // 防重id
		Content        string `db:"content"`    // 值
		Content1       string `db:"content1"`   // 值1
		Content2       string `db:"content2"`   // 值2
		Content3       string `db:"content3"`   // 值3
		Content4       string `db:"content4"`   // 值4
		IsDelete       int64  `db:"is_delete"`  // 1删除2未删除
		CreateTime     uint64 `db:"create_time"`
		UpdateTime     uint64 `db:"update_time"`
	}

	BaseCodeItemSearch struct {
		SortKey   string
		Key       string
		ForeignId string
		IsDelete  int64
		Page      int64
		PageSize  int64
		Content   string
		Content1  string
		Content2  string
		Content3  string
		Content4  string
	}
)

func newBaseCodeItemModel(conn sqlx.SqlConn, c cache.CacheConf, opts ...cache.Option) *defaultBaseCodeItemModel {
	return &defaultBaseCodeItemModel{
		CachedConn: sqlc.NewConn(conn, c, opts...),
		table:      "`base_code_item`",
	}
}

func (m *defaultBaseCodeItemModel) Delete(ctx context.Context, baseCodeItemId uint64) error {
	amigoBaseCodeItemBaseCodeItemIdKey := fmt.Sprintf("%s%v", cacheAmigoBaseCodeItemBaseCodeItemIdPrefix, baseCodeItemId)
	_, err := m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("delete from %s where `base_code_item_id` = ?", m.table)
		return conn.ExecCtx(ctx, query, baseCodeItemId)
	}, amigoBaseCodeItemBaseCodeItemIdKey)
	return err
}

func (m *defaultBaseCodeItemModel) FindOne(ctx context.Context, baseCodeItemId uint64) (*BaseCodeItem, error) {
	amigoBaseCodeItemBaseCodeItemIdKey := fmt.Sprintf("%s%v", cacheAmigoBaseCodeItemBaseCodeItemIdPrefix, baseCodeItemId)
	var resp BaseCodeItem
	err := m.QueryRowCtx(ctx, &resp, amigoBaseCodeItemBaseCodeItemIdKey, func(ctx context.Context, conn sqlx.SqlConn, v any) error {
		query := fmt.Sprintf("select %s from %s where `base_code_item_id` = ? limit 1", baseCodeItemRows, m.table)
		return conn.QueryRowCtx(ctx, v, query, baseCodeItemId)
	})
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultBaseCodeItemModel) FindOneBySortKeyKey(ctx context.Context, sortKey string, key string) (*BaseCodeItem, error) {
	cacheKey := fmt.Sprintf("cache:amigo:baseCodeItem:sortKey:key:%s:%s", sortKey, key)
	var resp BaseCodeItem
	err := m.QueryRowIndexCtx(ctx, &resp, cacheKey, m.formatPrimary, func(ctx context.Context, conn sqlx.SqlConn, v any) (i any, e error) {
		query := fmt.Sprintf("select %s from %s where `sort_key` = ? and `key` = ? limit 1", baseCodeItemRows, m.table)
		if err := conn.QueryRowCtx(ctx, &resp, query, sortKey, key); err != nil {
			return nil, err
		}
		return resp.BaseCodeItemId, nil
	}, m.queryPrimary)
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultBaseCodeItemModel) Insert(ctx context.Context, data *BaseCodeItem) (sql.Result, error) {
	// 自动设置创建和更新时间
	now := uint64(time.Now().Unix())
	data.CreateTime = now
	data.UpdateTime = now

	amigoBaseCodeItemBaseCodeItemIdKey := fmt.Sprintf("%s%v", cacheAmigoBaseCodeItemBaseCodeItemIdPrefix, data.BaseCodeItemId)
	ret, err := m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		// 重新计算字段列表，包含create_time和update_time
		insertFieldNames := stringx.Remove(baseCodeItemFieldNames, "`base_code_item_id`")
		insertFields := strings.Join(insertFieldNames, ",")
		placeholders := strings.Repeat("?,", len(insertFieldNames)-1) + "?"

		query := fmt.Sprintf("insert into %s (%s) values (%s)", m.table, insertFields, placeholders)
		// 根据字段顺序构造参数列表
		params := make([]interface{}, 0, len(insertFieldNames))
		for _, field := range insertFieldNames {
			switch field {
			case "`sort_key`":
				params = append(params, data.SortKey)
			case "`key`":
				params = append(params, data.Key)
			case "`foreign_id`":
				params = append(params, data.ForeignId)
			case "`content`":
				params = append(params, data.Content)
			case "`content1`":
				params = append(params, data.Content1)
			case "`content2`":
				params = append(params, data.Content2)
			case "`content3`":
				params = append(params, data.Content3)
			case "`content4`":
				params = append(params, data.Content4)
			case "`is_delete`":
				params = append(params, data.IsDelete)
			case "`create_time`":
				params = append(params, data.CreateTime)
			case "`update_time`":
				params = append(params, data.UpdateTime)
			}
		}
		return conn.ExecCtx(ctx, query, params...)
	}, amigoBaseCodeItemBaseCodeItemIdKey)
	return ret, err
}

func (m *defaultBaseCodeItemModel) Update(ctx context.Context, newData *BaseCodeItem) error {
	data, err := m.FindOne(ctx, newData.BaseCodeItemId)
	if err != nil {
		return err
	}

	// 自动设置更新时间
	now := uint64(time.Now().Unix())
	newData.UpdateTime = now

	amigoBaseCodeItemBaseCodeItemIdKey := fmt.Sprintf("%s%v", cacheAmigoBaseCodeItemBaseCodeItemIdPrefix, data.BaseCodeItemId)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		// 修改更新语句，包含update_time字段
		updateFieldNames := stringx.Remove(baseCodeItemFieldNames, "`base_code_item_id`", "`create_time`", "`create_at`", "`created_at`")
		updateFields := strings.Join(updateFieldNames, "=?,") + "=?"
		query := fmt.Sprintf("update %s set %s where `base_code_item_id` = ?", m.table, updateFields)

		// 根据字段顺序构造参数列表
		params := make([]interface{}, 0, len(updateFieldNames))
		for _, field := range updateFieldNames {
			switch field {
			case "`sort_key`":
				params = append(params, newData.SortKey)
			case "`key`":
				params = append(params, newData.Key)
			case "`foreign_id`":
				params = append(params, newData.ForeignId)
			case "`content`":
				params = append(params, newData.Content)
			case "`content1`":
				params = append(params, newData.Content1)
			case "`content2`":
				params = append(params, newData.Content2)
			case "`content3`":
				params = append(params, newData.Content3)
			case "`content4`":
				params = append(params, newData.Content4)
			case "`is_delete`":
				params = append(params, newData.IsDelete)
			case "`update_time`":
				params = append(params, newData.UpdateTime)
			}
		}
		params = append(params, newData.BaseCodeItemId)

		return conn.ExecCtx(ctx, query, params...)
	}, amigoBaseCodeItemBaseCodeItemIdKey)
	return err
}

func (m *defaultBaseCodeItemModel) formatPrimary(primary any) string {
	return fmt.Sprintf("%s%v", cacheAmigoBaseCodeItemBaseCodeItemIdPrefix, primary)
}

func (m *defaultBaseCodeItemModel) queryPrimary(ctx context.Context, conn sqlx.SqlConn, v, primary any) error {
	query := fmt.Sprintf("select %s from %s where `base_code_item_id` = ? limit 1", baseCodeItemRows, m.table)
	return conn.QueryRowCtx(ctx, v, query, primary)
}

func (m *defaultBaseCodeItemModel) tableName() string {
	return m.table
}

func (m *defaultBaseCodeItemModel) CheckDuplicate(ctx context.Context, data *BaseCodeItem) (bool, error) {
	// 首先通过 SortKey 和 Key 查找是否存在对应的记录
	existing, err := m.FindOneBySortKeyKey(ctx, data.SortKey, data.Key)
	if err == sqlc.ErrNotFound {
		// 没有找到，说明不重复
		return false, nil
	}
	if err != nil {
		// 查询出错
		return false, err
	}

	// 找到了对应的 SortKey 和 Key 记录
	if data.BaseCodeItemId == 0 {
		// 主键id为0，表示新增数据，只要SortKey和Key组合存在就认为是重复
		return true, nil
	}

	// 主键id不为0，表示更新数据，需要判断找到的记录是否是自己
	if existing.BaseCodeItemId != data.BaseCodeItemId {
		// 找到了其他记录拥有相同的SortKey和Key组合，认为是重复
		return true, nil
	}

	// 找到的记录就是自己，不是重复
	return false, nil
}

// List 方法根据搜索条件查询 BaseCodeItem 列表
func (m *defaultBaseCodeItemModel) List(ctx context.Context, search *BaseCodeItemSearch) ([]*BaseCodeItem, int64, error) {
	// 构建查询条件（参数化查询，防止 SQL 注入）
	var conditions []utils.SQLCondition

	if search.SortKey != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`sort_key` = ?", Args: []any{search.SortKey}})
	}
	if search.Key != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`key` = ?", Args: []any{search.Key}})
	}
	if search.ForeignId != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`foreign_id` = ?", Args: []any{search.ForeignId}})
	}
	if search.IsDelete != 0 {
		conditions = append(conditions, utils.SQLCondition{Clause: "`is_delete` = ?", Args: []any{search.IsDelete}})
	}
	if search.Content != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`content` like ?", Args: []any{fmt.Sprintf("%%%s%%", search.Content)}})
	}
	if search.Content1 != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`content1` like ?", Args: []any{fmt.Sprintf("%%%s%%", search.Content1)}})
	}
	if search.Content2 != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`content2` like ?", Args: []any{fmt.Sprintf("%%%s%%", search.Content2)}})
	}
	if search.Content3 != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`content3` like ?", Args: []any{fmt.Sprintf("%%%s%%", search.Content3)}})
	}
	if search.Content4 != "" {
		conditions = append(conditions, utils.SQLCondition{Clause: "`content4` like ?", Args: []any{fmt.Sprintf("%%%s%%", search.Content4)}})
	}

	queryWhere, whereArgs := utils.BuildWhereClause(conditions)

	// 获取总条数（不算分页的数据）
	countQuery := fmt.Sprintf("select count(*) from %s %s", m.table, queryWhere)

	var total int64
	if err := m.QueryRowNoCacheCtx(ctx, &total, countQuery, whereArgs...); err != nil {
		return nil, 0, fmt.Errorf("failed to get total count: %w", err)
	}

	// 构建查询语句（按主键id降序）
	pageClause, pageArgs := utils.DelSQLPage(search.Page, search.PageSize)
	query := fmt.Sprintf("select %s from %s %s order by `base_code_item_id` desc %s", baseCodeItemRows, m.table, queryWhere, pageClause)

	// 合并所有参数
	args := append(whereArgs, pageArgs...)

	// 执行查询
	var list []*BaseCodeItem
	if err := m.QueryRowsNoCacheCtx(ctx, &list, query, args...); err != nil {
		return nil, 0, err
	}

	return list, total, nil
}
