FROM alpine:latest

RUN apk add --no-cache tzdata
ENV TZ=Asia/Shanghai

WORKDIR /app
RUN mkdir etc
COPY deploy/simpleDockerFile/etc/deviceApi.yaml /app/etc/device.yaml
COPY deploy/simpleDockerFile/cmd/api/device /app/device

CMD ["./device", "-f", "etc/device.yaml"]