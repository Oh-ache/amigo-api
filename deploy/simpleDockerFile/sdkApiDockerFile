FROM alpine:latest

RUN apk add --no-cache tzdata
ENV TZ=Asia/Shanghai

WORKDIR /app
RUN mkdir etc
COPY deploy/simpleDockerFile/etc/sdkApi.yaml /app/etc/sdk.yaml
COPY deploy/simpleDockerFile/cmd/api/sdk /app/sdk

CMD ["./sdk", "-f", "etc/sdk.yaml"]